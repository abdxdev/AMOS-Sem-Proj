<Page
    x:Class="Automated_Menu_Ordering_System.Views.PizzasPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    DataContext="{Binding RelativeSource={RelativeSource Self}}"
    mc:Ignorable="d">

    <ScrollViewer VerticalScrollBarVisibility="Hidden">

        <Grid x:Name="ContentArea" RowSpacing="40">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Spacing="16">
                <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Specialty Pizzas" />
                <ScrollViewer
                    CornerRadius="8"
                    HorizontalScrollBarVisibility="Hidden"
                    VerticalScrollBarVisibility="Disabled">
                    <StackPanel
                        x:Name="SpecialtyPizzasPanel"
                        Orientation="Horizontal"
                        Spacing="24" />
                </ScrollViewer>
            </StackPanel>
            <StackPanel Grid.Row="1" Spacing="16">
                <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Classic Pizzas" />
                <ScrollViewer
                    CornerRadius="8"
                    HorizontalScrollBarVisibility="Hidden"
                    VerticalScrollBarVisibility="Disabled">
                    <StackPanel
                        x:Name="ClassicPizzasPanel"
                        Orientation="Horizontal"
                        Spacing="24" />
                </ScrollViewer>
            </StackPanel>
            <StackPanel Grid.Row="2" Spacing="16">
                <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Meat Lover's Pizzas" />
                <ScrollViewer
                    CornerRadius="8"
                    HorizontalScrollBarVisibility="Hidden"
                    VerticalScrollBarVisibility="Disabled">
                    <StackPanel
                        x:Name="MeatLoversPizzasPanel"
                        Orientation="Horizontal"
                        Spacing="24" />
                </ScrollViewer>
            </StackPanel>
            <StackPanel Grid.Row="3" Spacing="16">
                <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Vegetarian Pizzas" />
                <ScrollViewer
                    CornerRadius="8"
                    HorizontalScrollBarVisibility="Hidden"
                    VerticalScrollBarVisibility="Disabled">
                    <StackPanel
                        x:Name="VegetarianPizzasPanel"
                        Orientation="Horizontal"
                        Spacing="24" />
                </ScrollViewer>
            </StackPanel>

        </Grid>
    </ScrollViewer>
</Page>